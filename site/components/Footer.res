@react.component
let make = () => {
    <footer role="contentinfo" className="flex justify-center py-2 space-x-4 text-xs">
        <span>{React.string("Privacy")}</span>
        <span>{React.string("Sitemap")}</span>
    </footer>
}