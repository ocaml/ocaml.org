/*
	The code below is commented out because ReScript seems to be unable to export
	a default value when the name of the module begins with a number.

	I tried creating a 404.js file, but something kept automatically deleting it.
	So instead I settled on creating it as a ReScript file that is implemented
	entirely using %%raw
*/

// let default: unit => React.element

