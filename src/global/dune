(library
 (name ocamlorg)
 (public_name ocamlorg.global))

(rule
 (targets commit.ml)
 (action
  (with-stdout-to
   %{targets}
   (progn
    (echo "let hash = \"")
    (pipe-stdout
     (run "git" "rev-parse" "HEAD")
     (run "tr" "-d" "\n"))
    (echo "\"\n")))))
